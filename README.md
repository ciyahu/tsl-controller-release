# TSL 本地控制器 iOS — IPA 下载

这里只存放构建好的无签名 IPA，不含源码。每次构建会覆盖 `latest` Release。

**固定下载地址（免登录）：**

```
https://github.com/ciyahu/tsl-controller-release/releases/latest/download/TSLLocalController-unsigned.ipa
```

无签名 IPA 需要用自签工具（AltStore / SideStore / Sideloadly / ESign / TrollStore）签名后安装。
